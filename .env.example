# Scribo Environment Configuration Example
# Copy this file to .env and configure your values

# Cloudflare Push Relay Worker URL (for background push notifications via FCM)
VITE_PUSH_RELAY_URL=https://your-push-relay.workers.dev

# Optional: Enable Firebase Analytics (set to true if configured in Firebase Console)
VITE_ENABLE_ANALYTICS=false
